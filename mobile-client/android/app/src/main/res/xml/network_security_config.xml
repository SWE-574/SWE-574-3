<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext HTTP only to the Android emulator bridge + loopback so
         a release-mode APK can hit a backend running on the host during local
         Maestro runs. Real LAN devices should use `adb reverse tcp:8000
         tcp:8000` so `localhost` resolves to the host, instead of pinning a
         developer-specific IP here. Production builds shipped to
         apiary.selmangunes.com stay HTTPS-only by virtue of not matching
         any of these domains. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
